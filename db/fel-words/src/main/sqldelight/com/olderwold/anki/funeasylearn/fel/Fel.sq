CREATE TABLE Words (
  WordID INTEGER NOT NULL PRIMARY KEY,
  Adult INTEGER,
  Meaning TEXT
);

selectAll:
SELECT * FROM Words ORDER BY WordID;

findById:
SELECT * FROM Words WHERE WordID = ? ORDER BY WordID;
